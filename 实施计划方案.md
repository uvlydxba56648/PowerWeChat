# NFC URL Link 模块实施计划方案

## 项目概述

创建 `nfcUrlLink` 模块，实现微信小程序 NFC 专用 Scheme 接口（`wxa/generatenfcscheme`），用于生成可写入 NFC 标签的小程序跳转链接。

## 接口说明

### 接口基本信息
- **接口名称**: GenerateNFCScheme（获取用于 NFC 的小程序 scheme 码）
- **请求方式**: HTTPS POST
- **请求地址**: `https://api.weixin.qq.com/wxa/generatenfcscheme?access_token=ACCESS_TOKEN`
- **适用主体**: 仅面向国内非个人主体小程序开放
- **前置条件**:
  - 完成"设备接入"并获得 model_id
  - 在小程序后台申请「NFC 标签调起小程序」能力

### 请求参数
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| model_id | string | 是 | 设备接入审核通过后分配的设备型号ID |
| sn | string | 否 | 设备序列号，一机一码场景使用 |
| jump_wxa | object | 否 | 跳转配置 |
| jump_wxa.path | string | 否 | 已发布小程序页面路径，不可携带query |
| jump_wxa.query | string | 否 | 页面参数，最大1024字符 |
| jump_wxa.env_version | string | 否 | release/trial/develop，微信外打开时生效 |

### 返回参数
| 参数 | 类型 | 说明 |
|------|------|------|
| errcode | int | 错误码，0表示成功 |
| errmsg | string | 错误描述 |
| openlink | string | NFC 专用 Scheme，写入 NFC 标签的 URI |

### 常见错误码
- **48001**: 接口未授权/未开通「NFC 标签调起小程序」能力
- **40001/40013**: access_token 无效或主体不符
- **40165**: path 不合法
- **40212**: query 不合法

## 实施方案

### 一、目录结构

```
src/miniProgram/nfcUrlLink/
├── client.go                      # 客户端实现
├── provider.go                    # Provider注册
├── request/
│   └── requestNFCUrlLink.go      # 请求结构体定义
└── response/
    └── responseNFCUrlLink.go     # 响应结构体定义
```

### 二、文件详细设计

#### 1. client.go - 客户端实现

**文件路径**: `src/miniProgram/nfcUrlLink/client.go`

**功能说明**:
- 实现 `Client` 结构体，包含 `BaseClient` 字段
- 实现 `GenerateNFCScheme` 方法，调用微信接口生成 NFC Scheme
- 使用已封装的 `BaseClient.HttpPostJson` 方法发送请求

**关键代码结构**:
```go
type Client struct {
    BaseClient *kernel.BaseClient
}

func (comp *Client) GenerateNFCScheme(ctx context.Context, options *request.NFCUrlLinkGenerate) (*response.ResponseNFCUrlLink, error)
```

**技术要点**:
- 接口路径: `wxa/generatenfcscheme`
- HTTP方法: POST
- Content-Type: application/json

---

#### 2. provider.go - Provider注册

**文件路径**: `src/miniProgram/nfcUrlLink/provider.go`

**功能说明**:
- 实现 `RegisterProvider` 函数
- 初始化 `BaseClient` 并创建 `Client` 实例
- 遵循现有模块的注册模式

**关键代码结构**:
```go
func RegisterProvider(app kernel.ApplicationInterface) (*Client, error)
```

**技术要点**:
- 调用 `kernel.NewBaseClient(app, nil)` 创建基础客户端
- 错误处理: 返回初始化错误

---

#### 3. request/requestNFCUrlLink.go - 请求结构体

**文件路径**: `src/miniProgram/nfcUrlLink/request/requestNFCUrlLink.go`

**功能说明**:
- 定义 `NFCUrlLinkGenerate` 请求结构体
- 定义 `JumpWxa` 嵌套结构体

**数据结构**:

```go
// NFCUrlLinkGenerate NFC URL Link 生成请求
type NFCUrlLinkGenerate struct {
    ModelID  string    `json:"model_id"`           // 必填：设备型号ID
    SN       string    `json:"sn,omitempty"`       // 选填：设备序列号
    JumpWxa  *JumpWxa  `json:"jump_wxa,omitempty"` // 选填：跳转配置
}

// JumpWxa 跳转小程序配置
type JumpWxa struct {
    Path       string `json:"path,omitempty"`        // 已发布的小程序页面路径
    Query      string `json:"query,omitempty"`       // 页面参数，最大1024字符
    EnvVersion string `json:"env_version,omitempty"` // release/trial/develop
}
```

**字段说明**:
- `ModelID`: 设备接入审核通过后由平台分配
- `SN`: 一机一码场景的设备序列号，为空则为一型一码
- `JumpWxa`: 跳转小程序的配置
  - `Path`: 必须是已发布页面的有效路径，不可携带query参数
  - `Query`: 支持的字符集: 数字、大小写英文及 `! # $ & ' ( ) * + , / : ; = ? @ - . _ ~ %`
  - `EnvVersion`: 在微信外打开时生效

---

#### 4. response/responseNFCUrlLink.go - 响应结构体

**文件路径**: `src/miniProgram/nfcUrlLink/response/responseNFCUrlLink.go`

**功能说明**:
- 定义 `ResponseNFCUrlLink` 响应结构体
- 继承 `response.ResponseMiniProgram` 获得标准错误处理字段

**数据结构**:

```go
type ResponseNFCUrlLink struct {
    response.ResponseMiniProgram
    OpenLink string `json:"openlink"` // NFC 专用 Scheme (形如 weixin://...)
}
```

**继承字段** (来自 ResponseMiniProgram):
- `ErrCode`: 错误码，0表示成功
- `ErrMsg`: 错误描述信息

**字段说明**:
- `OpenLink`: NFC 专用的小程序 Scheme 链接，用于写入 NFC 标签的 URI 记录

---

### 三、集成到 MiniProgram 应用

#### 修改文件: `src/miniProgram/application.go`

**1. 添加 import**
```go
"github.com/ArtisanCloud/PowerWeChat/v3/src/miniProgram/nfcUrlLink"
```

**2. 在 MiniProgram 结构体中添加字段**
```go
type MiniProgram struct {
    // ... 现有字段 ...
    URLScheme *urlScheme.Client
    URLLink   *urlLink.Client
    NFCUrlLink *nfcUrlLink.Client  // 新增
    // ... 其他字段 ...
}
```

**3. 在 NewMiniProgram 函数中注册 provider**
```go
//-------------- register NFCUrlLink --------------
app.NFCUrlLink, err = nfcUrlLink.RegisterProvider(app)
if err != nil {
    return nil, err
}
```

插入位置: 在 `URLLink` 注册之后，`Security` 注册之前

**4. 在 GetComponent 函数中添加 case**
```go
case "NFCUrlLink":
    return app.NFCUrlLink
```

插入位置: 在 "URLLink" case 之后

---

## 使用示例

### 基本用法 - 一型一码

```go
import (
    "context"
    "github.com/ArtisanCloud/PowerWeChat/v3/src/miniProgram"
    "github.com/ArtisanCloud/PowerWeChat/v3/src/miniProgram/nfcUrlLink/request"
)

// 初始化小程序
app, err := miniProgram.NewMiniProgram(&miniProgram.UserConfig{
    AppID:  "your_app_id",
    Secret: "your_secret",
})

// 生成 NFC Scheme (一型一码)
result, err := app.NFCUrlLink.GenerateNFCScheme(context.Background(), &request.NFCUrlLinkGenerate{
    ModelID: "your_model_id",
    JumpWxa: &request.JumpWxa{
        Path:       "pages/index/index",
        Query:      "id=123&source=nfc",
        EnvVersion: "release",
    },
})

if err != nil {
    // 处理错误
}

// 使用返回的 openlink 写入 NFC 标签
nfcScheme := result.OpenLink  // 形如: weixin://...
```

### 高级用法 - 一机一码

```go
// 生成 NFC Scheme (一机一码，每个设备独立链接)
result, err := app.NFCUrlLink.GenerateNFCScheme(context.Background(), &request.NFCUrlLinkGenerate{
    ModelID: "your_model_id",
    SN:      "device_serial_001",  // 设备唯一序列号
    JumpWxa: &request.JumpWxa{
        Path:       "pages/device/detail",
        Query:      "sn=device_serial_001",
        EnvVersion: "release",
    },
})
```

### 错误处理

```go
result, err := app.NFCUrlLink.GenerateNFCScheme(ctx, options)
if err != nil {
    log.Printf("请求失败: %v", err)
    return
}

if result.ErrCode != 0 {
    switch result.ErrCode {
    case 48001:
        log.Println("未开通NFC标签调起小程序能力")
    case 40001, 40013:
        log.Println("access_token无效")
    case 40165:
        log.Println("页面路径不合法")
    case 40212:
        log.Println("查询参数不合法")
    default:
        log.Printf("错误: [%d] %s", result.ErrCode, result.ErrMsg)
    }
    return
}

// 成功获取 openlink
fmt.Printf("NFC Scheme: %s\n", result.OpenLink)
```

---

## 实施步骤

### 第一步: 创建目录结构
```bash
mkdir -p src/miniProgram/nfcUrlLink/request
mkdir -p src/miniProgram/nfcUrlLink/response
```

### 第二步: 创建文件（按顺序）
1. `src/miniProgram/nfcUrlLink/request/requestNFCUrlLink.go` - 定义数据结构
2. `src/miniProgram/nfcUrlLink/response/responseNFCUrlLink.go` - 定义响应结构
3. `src/miniProgram/nfcUrlLink/provider.go` - 实现 Provider
4. `src/miniProgram/nfcUrlLink/client.go` - 实现核心功能

### 第三步: 集成到应用
5. 修改 `src/miniProgram/application.go` - 添加字段、注册、组件访问

### 第四步: 测试验证
- 单元测试: 测试请求结构序列化
- 集成测试: 实际调用微信接口（需要有效的 model_id）
- 错误场景测试: 测试各种错误码处理

---

## 技术规范

### 命名规范
- 包名: `nfcUrlLink` (驼峰命名)
- 结构体: `NFCUrlLinkGenerate`, `ResponseNFCUrlLink` (大驼峰)
- JSON 标签: `model_id`, `jump_wxa`, `openlink` (下划线命名)
- 方法名: `GenerateNFCScheme` (大驼峰，公开方法)

### 代码风格
- 遵循 Go 标准代码规范
- 参考现有 `urlLink` 和 `urlScheme` 模块的实现风格
- 保持与项目整体风格一致

### 依赖管理
- 复用 `kernel.BaseClient` 的 HTTP 封装
- 继承 `response.ResponseMiniProgram` 的错误处理
- 不引入新的外部依赖

---

## 注意事项

### 权限要求
1. 小程序主体必须是**国内非个人主体**
2. 必须在小程序后台完成**设备接入**审核
3. 必须开通**「NFC 标签调起小程序」**能力
4. 获取有效的 `model_id`

### 参数限制
1. `Path`: 必须是已发布的有效页面路径，不可携带 query
2. `Query`: 最大 1024 字符，仅支持特定字符集
3. `ModelID`: 必填，由设备接入审核后分配
4. `SN`: 选填，填写后为一机一码，不填为一型一码

### NFC 写卡规范
1. 将 `OpenLink` 写入为 **NDEF/URI 记录**
2. 建议附加 **AAR 记录**: `com.tencent.mm` (提升安卓端成功率)
3. 写入后进行抽检验证

---

## 参考资料

### 官方文档
- 微信官方文档: https://developers.weixin.qq.com/miniprogram/dev/api-backend/open-api/url-scheme/urlscheme.generate.html
- NFC 标签调起小程序: 相关能力开通流程

### 项目内部参考
- `src/miniProgram/urlLink/` - URL Link 模块实现
- `src/miniProgram/urlScheme/` - URL Scheme 模块实现
- `src/kernel/baseClient.go` - HTTP 请求封装
- `src/kernel/response/wx.go` - 响应结构定义

---

## 版本信息

- **创建日期**: 2025-10-31
- **目标版本**: v3.4.26+
- **兼容性**: 向后兼容，不影响现有功能

---

## 附录: 完整代码清单

### A. 文件清单

1. **新建文件** (4个)
   - `src/miniProgram/nfcUrlLink/client.go`
   - `src/miniProgram/nfcUrlLink/provider.go`
   - `src/miniProgram/nfcUrlLink/request/requestNFCUrlLink.go`
   - `src/miniProgram/nfcUrlLink/response/responseNFCUrlLink.go`

2. **修改文件** (1个)
   - `src/miniProgram/application.go` (4处修改点)

### B. 代码行数估算
- 新增代码: 约 80-100 行
- 修改代码: 约 10-15 行
- 总计: 约 90-115 行

---

## 总结

本方案基于现有 `urlLink` 和 `urlScheme` 模块的成熟实践，采用一致的架构设计和代码风格，实现了 NFC 专用小程序 Scheme 接口的完整封装。实施后将为开发者提供简洁易用的 API，支持一型一码和一机一码两种场景，满足 NFC 标签调起小程序的业务需求。
